# /api/app — React Native Customer App API

This namespace is reserved for endpoints consumed by the
Wraptors customer app (React Native + Expo). These routes
are built in a separate dedicated session.

They use customer JWT auth — not staff portal auth.
Do not add staff portal logic here.
